apiVersion: v1
kind: Service
metadata:
  name: {{ include "bambulabs-exporter.fullname" . }}
  labels:
    {{- include "bambulabs-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "bambulabs-exporter.selectorLabels" . | nindent 4 }}
